---
import type { CollectionEntry } from 'astro:content'

interface Props {
  author: CollectionEntry<'author'>
}

const { author } = Astro.props

const { subtitle, twitter, linkedIn, github, avatar, bio, name } = author.data
---

<div class="mx-auto my-10 max-w-lg rounded-lg bg-white p-5 shadow-md">
  <img class="mx-auto h-32 w-32 rounded-full" src={avatar.src} alt="Profile picture" />
  <h2 class="mt-3 text-center text-2xl font-semibold">{name}</h2>
  <p class="mt-1 text-center text-gray-600">{subtitle}</p>
  <div class="mt-5 flex justify-center">
    <a href={twitter} class="mx-3 text-blue-500 hover:text-blue-700"> Twitter </a>
    <a href={linkedIn} class="mx-3 text-blue-500 hover:text-blue-700"> LinkedIn </a>
    <a href={github} class="mx-3 text-blue-500 hover:text-blue-700"> GitHub </a>
  </div>
  <div class="mt-5">
    <h3 class="text-xl font-semibold">Bio</h3>
    <p class="mt-2 text-gray-600">{bio}</p>
    <div class="flex justify-end">
      <button class="text-blue-500">
        <a href={`/blog/${author.id}`}>Leer más</a>
      </button>
    </div>
  </div>
</div>

<style></style>
